# Suggested Commands

## Development & Run Commands
- `npm run dev` - Start the Vite local development server.
- `npm run build` - Build the production-ready assets under the `dist` directory.
- `npm run preview` - Start a local server to preview the production build.

## Code Quality Commands
- `npm run lint` - Lint the codebase using ESLint.
